---
name: group-duty-orchestration
description: 当用户明确要求 AI 在群聊中建立跨话轮的有限期职责或主 Agent 需要治理自身注意力时使用：持续关注指定 QQ 成员、按 Cron 周期检查、有限期对象忽略、可选有限回响、操作员授权或限频；插件额外唤醒但无公开价值时可结构化让出。功能讨论、假设和单次回复不触发状态变更。
---

# 群聊临时职责编排

## 边界

本 Skill 定义“如何使用机制完成一种临时职责”，不定义插件代码能力。

插件代码提供四类运行机制和一个治理工具：

0. `manage_sender_activation_access`：仅 AstrBot 管理员可按当前群向指定 QQ ID
   授予有限期插件操作员权限；它不授予 AstrBot 管理员或跨群权限。
1. `manage_sender_activation`：某些对象发出普通群消息时，额外给原生主 Agent 一次判断机会。
2. `manage_sender_activation_rate`：只约束上述对象激活产生的额外机会。
3. `manage_heartbeat_lease`：按有限期时序，复用 AstrBot 原生 Cron 给原生主 Agent 判断机会。
4. `yield_current_turn`：在本插件额外激活的回合里，正式选择不占用可见话轮。
5. `manage_attention_ignore`：给主 Agent 一个由部署者配置授权的有限期对象忽略控制面。对象 ID、计数阈值和时间窗口可以叠加；它只统计原本可能激活主 Agent 的事件，命中后在 LLM 前尽量抑制后续成本。
6. `manage_echo_hook`：显式挂载有限回响。回响等于有限主动自激活权限；没有调用就没有回响。一次调用可以按配置预排有限次数，但所有次数都来自原激活回合，回响回合绝不能再创建回响。

职业、目标、现场判断、工具组合和退出条件属于主 AI 的高语境策略。具体应用场景没有封闭清单；示例只能帮助理解，不能反向成为程序分支或调用关键词。

## 编排过程

### 1. 从自然语言还原职责契约

先识别：

- 目标：有限期内希望群聊达到什么状态。
- 对象：哪些成员发言会带来新的判断必要性。
- 时序：即使没人发言，是否仍需定期检查。
- 信息条件：判断所需事实是否已在当前上下文；是否还要调用 QQ 检索、文件、网络或其他独立工具。
- 行动边界：可直接做什么，什么需要授权，什么只需报告。
- 退出条件：期限到达、目标完成、用户撤销、条件消失或状态异常。

不要把职业名称当作固定模板。相同名称在不同群里的对象、频率、信息条件、行动边界和退出条件可以完全不同。

### 2. 选择最少的机制组合

- 只有指定成员后续发言需要被看见：建立发言者激活租约。
- 只有按时检查需要：建立心跳租约。
- 两类触发都需要：分别建立，期限应与职责期限一致。
- 只需处理当前一条消息：不建立任何跨话轮租约。
- 需要降频：仅对已有对象激活租约添加从属限频；未明确需要时不默认限频。
- 已知某对象会造成无价值或对抗性激活成本：若部署者授予 Ignore 权限，可建立有限对象忽略。忽略与对象激活不是互相删除的状态；同一对象可以同时存在激活租约和更短的忽略策略，命中忽略时由忽略效果临时覆盖该次激活，忽略到期后原有限激活租约仍按自身期限继续。
- 需要在一次真实激活后保留短暂的主动再检查机会：只有确有必要时显式创建 Echo。Echo 不是每轮默认步骤，也不是长期追踪；没有钩子就不发生。

心跳不是群聊记录检索器。若一次判断必须读取心跳间隔内的新消息，应在该回合使用当前会话可用的正式 QQ 历史或检索工具；没有此工具时必须说明信息边界，不能假装已经观察。

### 2.1 区分三个句柄与两个竞争效果

对象、时间、计数是可组合的条件句柄，不是三个互斥模式。当前 rc19 首先把这种组合用于对象忽略：

- `对象 + 忽略`：`trigger_count=1`，忽略持续时间与策略期限按需要设置。
- `对象 + 计数 + 忽略`：对象累计到 N 个可激活事件后再忽略。
- `对象 + 时间窗口 + 计数 + 忽略`：只统计滑动时间窗口中的可激活事件。

激活与忽略才是竞争效果。已有 `对象 + 有限时间 + 激活` 的追踪对象，在刷屏时可以叠加更短的 `对象 + 时间/计数 + 忽略`，而不是销毁原追踪意图。当前版本不把尚未实现的全局时间忽略、通用计数激活或全量监听伪装成已有能力。

### 3. 用正式工具帧建立状态

用户自然语言不能直接触发程序。主 AI 理解完整语境后，才生成工具帧。

先确认当前行动主体是否可达：

- AstrBot 管理员可以直接使用全部状态工具，并可管理操作员授权。
- 当前群获授权的插件操作员可以使用对象激活、限频和心跳工具，但不能转授权。
- 未授权成员提出操作请求时，不应口头假装成功；应根据
  `operator_access_required` 简洁说明需要管理员授予本群权限。
- 若管理员明确提出授权，主 AI 直接调用 `manage_sender_activation_access`；
  不要求管理员改写成“使用插件”或手工 JSON。
- 任何正式主 Agent 回合都保留撤销/终止或收紧异常状态的一票否决，但不能
  把收敛权当成新增、续期、放宽或转授权的权限。

- 作用域来自当前事件，不能由用户或模型参数伪造。
- 发言者目标必须是可验证的数字 QQ ID，不用昵称或 `me` 等占位符。
- 心跳使用 AstrBot 五段 Cron，最小粒度为分钟。
- 所有租约必须有限期。
- `instruction` 写“本轮要依据什么目标和现场条件判断”，不要写死职业专用代码流程。
- 主动激活权限与主动忽略权限不对称：对象激活、心跳等扩大可达性的权限仍按现有管理员/操作员边界；对象忽略与 Echo 是否开放由插件配置决定，程序不根据模型名称自动提权。
- `ignore_native_wake_mode` 由部署者选择：`extra_only` 只压本插件额外激活；`suppress_llm`阻断 AstrBot 默认 LLM 但保留其他插件；`stop_event` 最省成本但会停止整个事件传播。不要把三者效果说成一样。
- 收到 `status=ok` 前，不得口头声称已经上岗或已经生效。
- 撤销/禁用只保证阻止尚未开始的未来激活。若原生 `run_job_now()` 已经开始执行主 Agent，当前在途一次可能完成；不得把随后成功取消说成“追回了已经开始的这一轮”。
- 对第一次出现、尚未建立 Ignore 策略的对抗对象，已经并发启动的 LLM 回合不能事后追回；有限 Ignore 保护的是策略生效后的后续可激活事件。
- 错误回执中的 `recovery_action` 只是下一步建议，不表示恢复动作已经执行。当 `recovery_action_executed=false` 时不得说“正在重载/已经修复”；当 `automatic_retry_scheduled=false` 时只能说“需要在恢复后再次调用/重试”，不得说“会重试”“将重试”或以其他方式暗示未来动作已经安排。只有后续真的执行了相应动作并取得新事实后，才能报告它已经发生。

若目标、对象或关键参数无法唯一确定，先用符合当前人设的自然语言澄清；能够从上下文唯一确定时直接调用，不做机械式重复确认。

### 4. 每次激活重新判断

激活只增加主 Agent 的可达性，不强制回复。

每一回合重新检查：

- 当前事实是否满足介入条件。
- 新信息是否足以改变既有判断。
- 是否有可执行且符合职责边界的动作。
- 发言是否会制造重复、打断、冲突或噪音。

有价值时正常发言或调用其他正式工具。没有公开价值不等于控制面什么都不能做：例如一次对抗性刷屏已经进入 Agent 后，可以先在一个工具选择中调用 `manage_attention_ignore`建立未来的有限前置忽略；如果最终仍不应公开回复，再在后续最后一次工具选择中把
`yield_current_turn` 作为唯一调用。成功调用会使用
AstrBot 原生本地工具的终结返回结束本轮，不再进入下一次工具选择，从而形成
真正的无可见回复，而不是发送“我保持沉默”。

不得先发送内容、点赞或执行其他外部动作，再用让出工具试图撤回；让出工具
不会撤销此前已经执行的外部动作。成功让出没有供模型继续处理的常规 JSON
回执；参数或作用域错误才返回结构化错误并允许修正。

### 4.1 Echo 是可选钩子，不是默认续命

Echo 只在主 Agent 明确调用 `manage_echo_hook(action=create)` 时存在。默认次数目前可配置为 1，但不是代码常量；`echo_max_count` 允许部署者决定一次原始激活最多预排多少个回响。若 count > 1，各次回响在原始激活回合一次性排入 AstrBot 原生 `run_once` 主动任务，不靠前一次回响创建后一次，因此仍然不是递归。

真实原生 @ 或被追踪对象的有效激活到来时，可以按配置取消当前作用域尚未执行的 Echo；如果一个恶意 @ 已被 Ignore 真正抑制，则它不应通过“取消 Echo”反过来形成拒绝服务。Echo 触发后应重新读取原会话语境；没有与自身或原目标相关的新对话时使用 `yield_current_turn`，而不是为了证明自己被唤醒而发言。

### 5. 分离控制面与对话面

租约的继续、续期、撤销、限频和修复属于**控制面**；面向群成员的事实、行动结果、必要澄清和有价值介入属于**对话面**。控制面判断不能因为发生在自然语言模型里，就自动成为群聊内容。

| 控制面结论 | 对话面有独立价值 | 正式行动 |
| --- | --- | --- |
| 继续现有租约 | 有 | 保持租约，正常提供有价值内容 |
| 继续现有租约 | 无 | 不改状态，调用 `yield_current_turn` |
| 自主终止对象或心跳租约 | 有 | 先正式 `disable`，取得回执后只说必要结果 |
| 自主终止对象或心跳租约 | 无 | 先正式 `disable` 并取得成功回执；下一次工具选择仅调用 `yield_current_turn` |
| 无法判断且必须由用户补充条件 | 有 | 提出最小必要澄清 |
| 无法判断且此刻无需打断群聊 | 无 | 调用 `yield_current_turn`，等待新事实 |

不得把“我在判断是否停止租约”“先继续观察”“本轮不停止”“暂时保留心跳”等内部维护过程发送到群聊。公开发言只能因为它本身对当前群聊有价值，不能因为模型需要把控制面思考说出来。

用户明确询问租约状态、明确要求启停，或撤销原因本身需要公开协调时，可以在正式工具回执后简洁说明结果；仍不得在调用前抢先声称状态已经改变。

### 6. 终止与修复

- 用户撤销、目标完成或存在足以证明语境不再适配的新事实：删除相应对象激活和心跳租约。
- 单条消息没有公开增量，只决定本轮让出，不等于目标完成，不得据此撤销租约。
- 对象激活异常密集：可以先建立有限期限频，或在完整语境表明租约本身错误时直接撤销。
- 程序结构故障触发还原报告：把报告当作事实，不当作重新建租约的命令。只有原需求仍然存在时才重建。
- 不得为无人要求的职责自动创建新租约。

租约到期就是默认退出。不要用永久状态模拟长期职责；需要继续时，基于当时的新语境显式续期。

## 开放示例

“临时值班”“持续接话”“观察某人后续观点”“每隔一段时间检查待办”“在现场需要时帮助协调”等都只是可能用途。任何新用途都应按上述契约重新组合，而不是等待 Skill 中出现相同词语。

最终效果可概括为：

`自然语言职责 → 主 AI 高语境理解 → 机制组合 → 每轮行动或让出 → 到期、完成或撤销`

这是一条类人生命周期闭环，不是职业枚举器。
